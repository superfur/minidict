# Changelog

## v2.6.0 (2026-06-23)
---
* **三种字典输出统一**
  - Bing/Youdao/Google 统一为相同结构：音标 → 词性释义 → 网络释义 → 例句
  - 网络释义标签统一为「网络」（Bing 原 `Web` 前缀已归一），多条合并去重为一行
  - 词性标签（int./n./v./adj. …）按显示列宽对齐
* **边框轮廓修复**
  - 改用 `boxen` 绘制所有卡片，借 `string-width` 正确处理中英文/全角宽度
  - 移除 emoji（🔍🔊⚠）——它们在不同终端宽度不一致，是右边框错位的根因，改用宽度恒为 1 的符号
  - 折叠源文本中的换行/制表符，避免破坏卡片布局
* **Google 增强**
  - 新增音标与按词性归类的释义（`dt=rm` / `dt=bd`），与其它词典结构一致
* **Bing / Youdao 短语句子修复**
  - Bing 短语/整句改用带令牌的 `ttranslatev3`（www.bing.com）真实机翻
  - Youdao 短语改为解析 jsonapi（释义/音标/网络/例句）；整句给出明确降级提示
* **新增自动更新**
  - 查询后静默检查新版本（默认开启，24h 节流，CI 与 `--no-update-check` / `MINIDICT_NO_UPDATE_CHECK` / 配置可关闭）
  - `dict -u` / `dict --update`：检测包管理器（npm/pnpm/yarn/bun）并执行对应全局升级
  - 当新版本要求的 Node 版本高于当前、或所需包管理器缺失时，明确告知问题
* **发布流程**
  - 新增 tag 触发的 GitHub Actions release workflow（push `v*` → lint/test/build → 校验版本 → `npm publish`）

## v2.4.3 (2026-04-25)
---
* **调整默认超时时间**
  - 所有插件（Bing、有道、Google）的默认请求超时从 3000ms 提高到 10000ms（10秒）
  - 解决通过代理访问 Google 翻译时连接超时的问题

## v2.4.2 (2026-04-25)
---
* **CLI 输出全面优化**
  - 全新卡片/边框风格，使用 Unicode box-drawing 字符（╭╮╰╯│─）清晰分隔各插件结果
  - 丰富的颜色区分：查询词（青色）、音标（品红）、词性标签（蓝色）、翻译文本（白色）、插件名（黄色）、成功/失败状态（绿/红）
  - 长文本自动换行，边框始终对齐，不再出现内容溢出
  - 音标信息统一在头部卡片中显示，避免重复
  - 错误结果使用独立卡片样式，带 ⚠ 图标和红色提示

* **修复进程不退出的问题**
  - 查询完成后显式调用 `process.exit(0)`，避免 HTTP keep-alive 连接导致进程挂起

## v2.4.0 (2026-04-24)
---
* **Google 翻译快速失败**
  - 添加请求超时机制，默认 3000ms
  - Google 翻译在国内网络环境下超时后立即返回，避免长时间挂起
  - 新增 `--timeout` 命令行选项和 `timeout` 配置项
* **并行查询 + 渐进输出**
  - 所有插件并行发起请求，大幅提升查询速度
  - 每个插件完成后立即显示结果，无需等待全部完成
  - 优化汇总信息，显示成功/失败插件数量
* **CLI 显示优化**
  - 失败插件显示友好的错误提示（如 "翻译服务暂不可用"）
  - 改进结果展示格式，更清晰易读
* **项目结构优化**
  - 删除冗余的类型定义（src/types/index.ts）
  - 删除未使用的文件和目录（src/core/, src/utils/const.ts 等）
  - 统一 fetch 工具，提取公共代码到 src/utils/fetch.ts
  - 清理旧的 lib/ 编译输出目录
* **配置增强**
  - Config 类型新增 `timeout` 字段
  - DictionaryPlugin 接口新增 `setTimeout` 方法
  - 所有插件支持超时配置

## v2.3.1 (2025-11-03)
---
* **文档增强**
  - 重写并扩展 README：新增快速开始、CLI 用法、编程接口、插件架构、故障排查与 FAQ 等章节
  - 明确 Node 版本与配置覆盖优先级
* **小改进**
  - 细化配置项说明与示例
  - 补充使用示例，提升可读性

## v2.3.0 (2025-10-31)
---
* **重大改进：统一配置文件格式**
  - 统一使用 JSON 格式配置文件（`~/.minidict.json`）
  - 更新 README 文档，确保配置说明与代码一致
  - 完善默认配置项（showPhonetic, showExamples, maxExamples）

* **类型系统改进**
  - 修复 TranslationResult 类型定义，统一音标和例句格式
  - 音标支持字符串和对象格式（{uk?, us?}）
  - 例句统一为 {en, zh} 结构
  - 统一字段名：使用 pluginName 替代 source

* **配置选项完善**
  - `--max-examples` 选项现在真正生效
  - 命令行选项可以覆盖配置文件设置
  - 完善配置项的默认值处理

* **解析器健壮性增强**
  - 为必应词典解析器添加多个备选 CSS 选择器
  - 增强错误处理和边界情况处理
  - 添加翻译结果和例句的去重和过滤逻辑
  - 提高对网站结构变化的适应性

* **测试改进**
  - 更新所有测试用例以匹配新的代码结构
  - 修复测试导入路径
  - 使用 node-fetch mock 替代 axios mock
  - 删除不存在的插件测试（google）

* **构建和工程化改进**
  - 优化构建脚本，移除不必要的 touch 命令
  - 提升 Node.js 版本要求至 16.0.0（配合 node-fetch@3）
  - 改进错误处理和信息提示

## v2.2.1 (2025-03-11)
---
* 移除 update-notifier 依赖，优化启动速度
* 修复版本号显示问题
* 优化错误提示信息

## v2.2.0 (2025-03-11)
---
* 优化依赖结构，移除冗余依赖
* 降低 Node.js 版本要求至 12.0.0
* 改进有道词典和必应词典的翻译质量
* 优化短语和句子翻译功能
* 改进错误处理和提示信息
* 优化代码结构和类型定义

## v2.1.0 (2025-03-10)
---
* 优化代码结构和错误处理机制
* 改进翻译服务的可靠性
* 添加更友好的中文提示信息
* 优化插件加载机制
* 改进类型定义和代码安全性
* 添加自动构建脚本

## v2.0.1 (2020-11-17)
---
* 修复有道的显示bug

## v2.0.0 (2020-11-10)
---
* 使用ts重写代码
* 优化模块

## v1.0.1 (2018-11-16)
---
* 增加指令
* 增加bing字典

## v1.0.0 (2018-11-04)
---
* 发布第一个可用的版本